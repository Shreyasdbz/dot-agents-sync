{
  "id": "travel",
  "description": "Optional Travel Planner agent and Trip Publish skill; private traveler context requires separate selection and grants.",
  "packages": [
    "agent.travel-planner",
    "skill.trip-publish"
  ]
}
