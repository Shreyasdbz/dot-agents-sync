{
  "apiVersion": "dasync.dev/v1",
  "kind": "Skill",
  "id": "skill.trip-publish",
  "name": "trip-publish",
  "version": "1.2.0",
  "description": "Turn an approved itinerary into a mobile-friendly HTML travel guide. Creating the file is distinct from hosting or sharing it.",
  "presentation": {
    "display_name": "Trip Publish",
    "short_description": "Publish approved itineraries as mobile HTML guides",
    "default_prompt": "Use $dasync-skill-trip-publish to turn this approved itinerary into a mobile-friendly HTML travel guide."
  },
  "license": "UNLICENSED",
  "entry": "SKILL.md",
  "files": [
    "SKILL.md"
  ],
  "scopes": [
    "user",
    "project"
  ],
  "token_budget": 1500,
  "requires": {
    "template.trip-publish": ">=1,<2",
    "policy.accessibility": ">=1,<2",
    "policy.communication": ">=1,<2",
    "agent.travel-planner": ">=1,<2"
  },
  "capabilities": [
    "filesystem.read"
  ],
  "prefers": [
    "browser.render",
    "agents.parallel"
  ],
  "fallback": "Apply the Travel Planner role inline if delegation is unavailable; structurally inspect HTML and disclose unperformed visual checks.",
  "outputs": [
    "html"
  ],
  "suggests": []
}
