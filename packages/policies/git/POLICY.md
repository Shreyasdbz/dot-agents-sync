# Git hygiene

Preserve dirty work. Do not reset or overwrite unrelated edits. Use explicit paths for staging and inspect the staged diff. Commit and publish only within granted authority. Never include credentials, private context, build caches, or incidental logs.
