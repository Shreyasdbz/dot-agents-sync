# Artifact forward test: raw responses and observed actions

Read only the four requested catalog SKILL.md files and their explicitly linked templates in /Users/shreyas/Dev/repos/dot-agents-sync/packages. Reference paths were resolved to packages/templates/pitch-deck/deck.html, packages/templates/trip-plan/itinerary.md, packages/templates/trip-publish/trip.html and packages/templates/playlist/playlist.md. No repository or user configuration files were edited. No browsing, external writes, hosting, account changes or bookings occurred.

Output directory: /private/tmp/dasync-artifact-forward.QaeVOd

## L — pitch-deck raw delivery response

Created a four-slide, self-contained HTML deck for engineering leads: /private/tmp/dasync-artifact-forward.QaeVOd/queue-retry.html

The deck recommends a three-day sandbox pilot built by one engineer. It distinguishes the observed seven-day baseline of 200 failures per 10,000 from the proposed threshold of under 50 per 10,000 without duplicate charges. It states that 12 mocked tests have passed, no load or production trial has run, and no measured result exists for the proposal. Measurement duration remains unspecified.

Used the supplied shell, retaining navigation, focus styles, reduced-motion handling and print rules. Local structural verification checked four sections, unique IDs, fragment targets, embedded JavaScript syntax, no external dependencies, viewport metadata and print rules. An initial duplicate ID between a slide and the Next button was found and corrected; the repeated check completed successfully. Browser rendering was unavailable: keyboard/pointer behavior, viewport/zoom overflow and printed appearance remain unverified. No claim of visual correctness is made.

## M — trip-plan raw delivery response

The temple visit does not fit the stated morning: leaving at 10:00 with 50 minutes of transit reaches it at 10:50, 20 minutes after the supplied 10:30 last admission, before allowing any buffer.

Keep the 10:00 departure and noon lunch. Travel toward lunch, take an optional short walk nearby only if the route leaves time, and aim to arrive by 11:45. After lunch, leave room for nearby sightseeing once hours, route and remaining budget are checked. Defer this temple if the 10:00 departure is fixed. If departure can change, 09:25 would yield an estimated 10:15 arrival, but opening time, visit duration and onward travel still need checking before accepting that alternative.

Treat JPY 5,000 as the total available until the per-person/total basis is clarified. Confirm lunch and transport costs first; use the remaining amount for admission and contingency. No current price or opening time has been verified. Date, named locations and traveler requirements are missing, so the route is provisional. No bookings were made.

Full template-based plan: /private/tmp/dasync-artifact-forward.QaeVOd/kyoto-plan.md

## N — trip-publish raw delivery response

Created the local shareable guide: /private/tmp/dasync-artifact-forward.QaeVOd/kyoto-share.html

It contains the public highlight “Kyoto temples and lunch.” No detailed stops, times, map destinations or booking status were supplied for this scenario, so none were invented. The full HTML was inspected with a local content check confirming all three supplied private values are absent, including from markup and scripts. The shell retains theme controls, accessible focus, local section links and print styles.

Unique IDs, valid local anchor targets, JavaScript syntax, viewport/print rules and absence of external dependencies were checked locally. Browser rendering, theme behavior, keyboard interaction and visual overflow remain unverified. The file was not hosted, uploaded or sent.

## O — curate-am-playlist raw delivery response

Preview only: start with Song One — Artist A (studio, supplied ISRC X1), then Song One Live — Artist A (live, supplied ISRC X2). The sequence introduces the studio recording before its distinct live counterpart; audio transitions and duration have not been verified.

The repeated studio record with ISRC X1 is omitted as an exact duplicate of the supplied recording. The live recording is retained as a different version. These are supplied metadata, not verified Apple Music identifiers. No connector, verified storefront IDs, URLs, lengths or availability are available. No playlist was created and no account changes were attempted.

Full durable preview: /private/tmp/dasync-artifact-forward.QaeVOd/playlist-preview.md

## Actual local verification output

queue-retry.html: unique IDs, local anchors, JS syntax, no external assets, viewport and print rules checked; sections=4

kyoto-share.html: unique IDs, local anchors, JS syntax, no external assets, viewport and print rules checked; sections=0

Share file: all three supplied private values absent from full file.

The guide uses two article elements rather than deck section elements. These are local structural/content observations, not browser execution or native-provider certification.
