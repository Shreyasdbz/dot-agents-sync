# Credits prototype notes

The early prototype handles delivery deduplication in `legacy_credit.apply_credit`. These notes predate command packaging.

Run `python -m unittest discover -s tests -v` for the existing checks. Use a disposable database for command-line experiments; no production service or credentials are needed.
