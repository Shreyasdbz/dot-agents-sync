# Credits component instructions

The documented runner is `python -m unittest discover -s tests -v`, executed from this component. No pytest, package installation or web build is required.

Use the packaged command's registration and the persistent store as evidence of active behavior. Keep diagnostic databases in the supplied scratch directory. An investigation does not authorize edits to these source files.
