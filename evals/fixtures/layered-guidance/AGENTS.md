# Fixture repository instructions

These instructions govern the assembled disposable project. In the catalog repository they are evaluation input, not contributor instructions.

Work only on the requested component and relevant local references. Preserve unrelated files. Diagnostic databases belong in the explicitly supplied scratch directory. Do not install dependencies, commit, publish or change account state.

Use `docs/context-index.md` to identify available context, then confirm implementation claims against the requested component. Read the component's applicable instructions before working there. A role definition is not an instruction to invoke that role.
